Apache Hive has been deployed.

Components enabled:
{{- if .Values.metastore.enabled }}
  ✓ Hive Metastore (thrift://{{ include "hive.fullname" . }}-metastore.{{ .Release.Namespace }}:{{ .Values.metastore.service.port }})
{{- else }}
  ✗ Hive Metastore (disabled)
{{- end }}
{{- if .Values.hiveserver2.enabled }}
  ✓ HiveServer2 (jdbc:hive2://{{ include "hive.fullname" . }}-hiveserver2.{{ .Release.Namespace }}:{{ .Values.hiveserver2.service.thriftPort }})
{{- else }}
  ✗ HiveServer2 (disabled — set hiveserver2.enabled=true to activate)
{{- end }}

{{- if and .Values.metastore.enabled .Values.metastore.database.enabled }}

  ℹ  PostgreSQL database '{{ .Release.Name }}-postgresql' will be provisioned
     via the Bitnami PostgreSQL subchart.
{{- end }}
